<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Manage News</title><link rel="stylesheet" href="/assets/css/style.css"></head><body class="dashboard-body"><main class="container py-4"><h1>Manage News</h1>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<table class="table"><tbody>@foreach($news as $item)<tr><td>{{ $item->title }}</td><td>{{ $item->status }}</td><td>@if($item->status!=='published')<form method="post" action="{{ route('admin.news.publish',$item) }}" class="d-inline">@csrf @method('PATCH')<button class="btn btn-sm btn-success">Publish</button></form>@endif<form method="post" action="{{ route('admin.news.destroy',$item) }}" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Delete</button></form></td></tr>@endforeach</tbody></table>{{ $news->links() }}</main></body></html>
